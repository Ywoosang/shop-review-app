spring:
    datasource:
        url: jdbc:mysql://db:3306/${DB_NAME}?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
        username: ${DB_USERNAME}
        password: ${DB_PASSWORD}
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: create
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQLDialect
                show_sql: true
                format_sql: true
logging:
    level:
        org.hibernate.SQL: debug
        org.hibernate.type: trace


